import { render, screen } from '@testing-library/react';

import ModalTwo from './ModalTwo';

describe('ModalTwo Component', () => {
  it('render the ModalTwo component', () => {
    render(<ModalTwo />);

    //
  });
});
