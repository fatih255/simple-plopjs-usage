import { render, screen } from '@testing-library/react'

import {{pascalCase name}} from './{{pascalCase name}}'

describe('{{pascalCase name}} Component', () => {
  it('render the {{pascalCase name}} component', () => {
    render(<{{pascalCase name}} />)

    //
  })
})